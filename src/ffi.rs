use crate::export_ffi_fn;

export_ffi_fn!(root, root);
export_ffi_fn!(user_list, user_list);
export_ffi_fn!(signup, signup);
export_ffi_fn!(login, login);
export_ffi_fn!(get_user, get_user);
export_ffi_fn!(init, init);
